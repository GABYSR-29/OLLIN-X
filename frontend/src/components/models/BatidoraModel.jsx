/** Metadatos del tipo batidora — asset: mezclar.glb (carga decorativa con GLTFLoader). */
export const batidoraModelSpec = {
  id: 'batidora',
  url: '/models/mezclar.glb',
  position: [-2, 0, 0],
  scale: 1,
};

export default batidoraModelSpec;
