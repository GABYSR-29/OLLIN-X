/* En desarrollo (Vite sirve este archivo): base vacía → se usa proxy o VITE_API_URL en build.
   En producción, server.js puede sobrescribir vía GET /runtime-config.js dinámico. */
window.__OLLINX_API_BASE__ = '';
